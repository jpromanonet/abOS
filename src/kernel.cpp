// We start by declaring the main kernel function of abOS

void kernelMain()
{
    // Just a test message
    printf("Hellow World! | https://juanpr.com");

    // We use a infinit loop becase we don't want our kernel to stop
    while(1);
}